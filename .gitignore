Thumbs.db
.DS_Store
*.tmp

# ---------------------------------------------------------------------------
# PRIVATE. This repository publishes to footjar.com -- anything committed here
# is readable by anyone who guesses the URL. Financial records, planning
# documents and working files live in _private/ and must never be tracked.
# Move that folder out of the repo when convenient; nothing depends on it.
# ---------------------------------------------------------------------------
_private/
